# Required
flask>=3.0
pefile>=2023.2.7

# Optional but strongly recommended — enables YARA pattern rules.
# If the wheel fails to build on Windows, the scanner still works without it.
yara-python>=4.3.0

# Optional — the desktop review app window ('python -m sentry app').
# Without it the app command falls back to opening the browser.
pywebview>=5.0

